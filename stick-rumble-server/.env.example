# Optional: HTTP/WebSocket port for the Go server.
PORT=8080

# Optional: enable outgoing schema validation for development/debugging.
ENABLE_SCHEMA_VALIDATION=false

# Future-facing deployment config. Leave blank for local MVP development.
GO_ENV=development
ALLOWED_ORIGINS=
