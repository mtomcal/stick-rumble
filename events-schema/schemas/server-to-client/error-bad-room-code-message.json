{
  "$id": "error_bad_room_codeMessage",
  "description": "error:bad_room_code WebSocket message",
  "type": "object",
  "required": [
    "type",
    "timestamp",
    "data"
  ],
  "properties": {
    "type": {
      "description": "Message type identifier",
      "const": "error:bad_room_code",
      "type": "string"
    },
    "timestamp": {
      "description": "Unix timestamp in milliseconds",
      "minimum": 0,
      "type": "integer"
    },
    "data": {
      "$id": "ErrorBadRoomCodeData",
      "description": "Bad room code rejection payload",
      "type": "object",
      "required": [
        "reason"
      ],
      "properties": {
        "reason": {
          "description": "Normalization failure reason",
          "anyOf": [
            {
              "const": "missing",
              "type": "string"
            },
            {
              "const": "too_short",
              "type": "string"
            },
            {
              "const": "too_long",
              "type": "string"
            }
          ]
        }
      }
    }
  }
}
