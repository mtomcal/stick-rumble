{
  "$id": "room_joinedMessage",
  "description": "room:joined WebSocket message",
  "type": "object",
  "required": [
    "type",
    "timestamp",
    "data"
  ],
  "properties": {
    "type": {
      "description": "Message type identifier",
      "const": "room:joined",
      "type": "string"
    },
    "timestamp": {
      "description": "Unix timestamp in milliseconds",
      "minimum": 0,
      "type": "integer"
    },
    "data": {
      "$id": "RoomJoinedData",
      "description": "Room joined event payload",
      "type": "object",
      "required": [
        "roomId",
        "playerId",
        "mapId"
      ],
      "properties": {
        "roomId": {
          "description": "Unique identifier for the room",
          "minLength": 1,
          "type": "string"
        },
        "playerId": {
          "description": "Unique identifier for the player",
          "minLength": 1,
          "type": "string"
        },
        "mapId": {
          "description": "Selected shared map identifier for the room",
          "minLength": 1,
          "type": "string"
        }
      }
    }
  }
}
